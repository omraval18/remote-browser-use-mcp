# Shadow DOM

Start with screenshots and coordinates. If text is hidden inside shadow roots, load DOM tools.

```python
load_skill("dom_tools")
text = deep_text()
clicked = click_text("Accept all")
```

Only use DOM traversal when visible coordinate input is not enough.
